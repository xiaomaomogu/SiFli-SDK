# .gitattributes template

# Set default behavior to automatically normalize line endings
* text=auto

# Define binary files to avoid automatic line ending normalization
*.png binary

# Define language-specific settings
*.c text
*.py text

# Custom settings for specific files
*.sh eol=lf
*.men eol=crlf  # e.g. t32.men